<%= form_for(@picture) do |f| %>
  <% if @picture.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@picture.errors.count, "error") %> prohibited this picture from being saved:</h2>

      <ul>
      <% @picture.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
      <%= f.label :name, "名前" %><br>
      <%= f.text_field :name , :class => "form-control", :style => 'width: 400px;display: inline-block;'%>
  </div>
  <div class="field">
    <%= f.label :title, "タイトル"%><br>
      <%= f.text_field :title , :class => "form-control", :style => 'width: 400px;display: inline-block;'%>
  </div>
  <div class="field">
    <%= f.label :description, "説明"%><br>
    <%= f.text_area:description  , :size => "20x6", :class => "form-control", :style => 'width: 400px;display: inline-block;'%>
  </div>
   <div class="field">
      <%= f.file_field :photo , :class => "form-control", :style => 'width: 400px;display: inline-block;'%>
    </div>
    <br><br>
  <div class="actions">
      <%= f.submit :class => 'btn btn-primary' %>
  </div>
<% end %>
